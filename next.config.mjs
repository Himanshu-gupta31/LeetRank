/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      runtime: 'nodejs', // Set runtime to Node.js for compatibility with Clerk
    },
  };
  
  export default nextConfig;
  